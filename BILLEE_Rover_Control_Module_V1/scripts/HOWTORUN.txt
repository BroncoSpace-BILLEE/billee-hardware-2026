From Project Root

On windows powershell:
.\kicad-venv\Scripts\Activate.ps1

On git bash windows:
source kicad-venv/Scripts/activate

On Linux
source kicad-venv/bin/activate

Be careful as running the script will overwrite your internal libraries, so custom symbols will be overwritten.
